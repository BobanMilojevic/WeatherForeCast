<Window x:Class="Wifi.WeatherForeCast.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Wifi.WeatherForeCast.Ui.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="800"
        Width="450"
        WindowStartupLocation="CenterScreen">
    
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0"
                   LastChildFill="True">
            
            <TextBox DockPanel.Dock="Top" 
                     Margin="5"
                     FontSize="20"
                     Text="{Binding SearchValue}"/>
        </DockPanel>

        <DockPanel Grid.Row="1" LastChildFill="True">

            <Image DockPanel.Dock="Top"
                   Source="pack://siteoforigin:,,,/Resources/weathericon/fog.png"/>
            <Label DockPanel.Dock="Top" 
                   Content="Felkirch"/>
            <Label DockPanel.Dock="Top" Content="Temperatur"/>
            <Label DockPanel.Dock="Top" Content="Luftdruck"/>
            <Label DockPanel.Dock="Top" Content=""/>
        </DockPanel>
        
    </Grid>
</Window>
